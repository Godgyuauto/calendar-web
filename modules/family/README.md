# modules/family

Family-scope data handling for events and shift overrides.

## Subfolders

- `domain`: in-memory store and CRUD logic.
- `api`: request parsing/validation and HTTP response mapping.

## Export contract

Use `@/modules/family` from callers. Keep barrel compatibility stable.
